[Unit]
Description=wazo-setupd daemon
After=network.target
Before=monit.service

[Service]
ExecStartPre=/usr/bin/install -d -o wazo-setupd -g wazo-setupd /var/run/wazo-setupd
ExecStart=/usr/bin/python3 /usr/bin/wazo-setupd

[Install]
WantedBy=multi-user.target
